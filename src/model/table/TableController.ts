import { TableInterface } from "./TableInterface";

import { FastifyReply, FastifyRequest } from "fastify";

export default class TableControllerClass{
    
    public async getAllTables(req : FastifyRequest,res : FastifyRequest){
        // Implementação
    }
}